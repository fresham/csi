%table
  - @questions.each do |question|
    %tr
      %td
        = link_to question.question, question
      %td
        = link_to 'Destroy', question_path(question), :confirm => 'Are you sure?', :method => :delete

%br

= link_to 'New Question', new_question_path
